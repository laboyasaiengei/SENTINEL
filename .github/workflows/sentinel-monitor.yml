name: SENTINEL Monitor Test

on:
  workflow_dispatch:
  schedule:
    - cron: "7/5 * * * *"

jobs:
  heartbeat:
    runs-on: ubuntu-latest
    steps:
      - name: SENTINEL heartbeat
        run: |
          echo "SENTINEL monitor alive"
          echo "Executed at: $(date -u +"%Y-%m-%dT%H:%M:%SZ")"